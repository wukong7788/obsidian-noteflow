{
  "name": "NoteFlow",
  "version": "0.2.3",
  "description": "Obsidian plugin: convert notes to WeChat HTML and XHS plain text",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.21.0",
    "eslint": "^8.57.0",
    "obsidian": "latest",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}